pub fn run1(lines: &mut super::LinesIterator) -> String {
    for line in lines {
        println!("{}", line.unwrap());
    }

    String::new()
}

pub fn run2(lines: &mut super::LinesIterator) -> String {
    for line in lines {
        println!("{}", line.unwrap());
    }

    String::new()
}
